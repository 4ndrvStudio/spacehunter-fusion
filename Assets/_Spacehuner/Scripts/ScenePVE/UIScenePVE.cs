using SH.Networking.PVE;
using UnityEngine;

namespace SH
{
    public class UIScenePVE : MonoBehaviour
    {
        private void Update()
        {
            //if (RoomPVEManager.Instance != null)
            //    UIManager.Instance.SetPing(RoomPVEManager.Instance.Ping);
        }
    }
}
